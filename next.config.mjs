/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      //todo
      //      "whatever the hostnamein the error message",
    ],
  },
}

export default nextConfig
